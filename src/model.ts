interface UserInfo {
    collections: {answerContent: string}[]
}

interface AnswerInfo {
    factoryCode: string
    answerId: string
    feeAmount: number
    examState: string
    payState: string
    userId: string
}
